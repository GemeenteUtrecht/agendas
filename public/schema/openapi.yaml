openapi: 3.0.2
info:
  description: |
    # Welkom innovator!
    Fijn dat je de tijd en moeite hebt genomen om je te verdiepen in dit component. Het "agenda" component is ontwikkeld voor de gemeente Utrecht en heeft als doel een agenda te koppelen aan objecten uit andere componenten. Dit stelt je in staat om aan deze objecten, afspraken en beschikbaarheid te kunnen beheren.
    
    # Ondersteuning
    Word lid van het slackkanaal [https://zaakonline.slack.com](https://zaakonline.slack.com) voor ondersteuning door het ontwikkel team. En krijg regelmatig updates over de ontwikkelingen op het gebied van de zaakonline componenten.
                 
    # Common ground en open source
    Om het voor alle partijen zo makkelijk mogelijk te maken, stellen we zo veel mogelijk informatie over dit component openbaar beschikbaar.
    - [datamodel als pdf](/schema/datamodel.pdf)
    - [datamodel als mwb](/schema/datamodel.mwb)
    - [codebase op github](https://github.com/GemeenteUtrecht/agendas)
    - [codebase als zip](https://github.com/GemeenteUtrecht/agendas/archive/master.zip)
    - [docker container](https://hub.docker.com/r/huwelijksplanner/agendas-component)
    - [online demo](http://agenda.demo.zaakonline.nl/)
    
    # OpenAPI Specification
    Deze API is gedocumenteerd in het **OpenAPI format** 
    De API bevat Cross-Origin Resource Sharing (CORS) en is geïmplementeerd volgens de [W3C spec](https://www.w3.org/TR/cors/) standaarden. De API staat cross-domain communicatie van de browser toe. Alle responses hebben een "wildcard same-origin", dit maakt ze openbaar en dus toegankelijk voor iedereen. Dat geldt voor elke code, op elke site.
    
    # Filteren en Sorteren
    Het is mogelijk om resultaten van deze API te filteren aan de hand van query parameters, deze worden meegegeven in de url tijdens het aanroepen van een endpoint, door extra waarden toe te voegen na een vraagteken. Bijvoorbeeld: ?bronOrganisatie=12345689 (*volledige endpoint  /agenda?bronOrganisatie=12345689*), zorgt ervoor dat alleen agendas met bronOrganisatie (ofwel rsin/organisatie) 123456789 worden weergegeven.
    
    Voor het filteren op datum geldt dat er een weegmethode moet worden meegegeven. Voor deze waardes geldt dat elke door [PHP datetime](https://php.net/manual/en/datetime.construct.php) ondersteunde waarde kan worden gebruikt. Verschillende methoden kunnen worden gebruikt om een spreiding te selecteren (bijvoorbeeld: */agenda?wijzigingsdatum[after]=2019-01-01&wijzigingsdatum[before]=2019-02-01*, om alle agendas gewijzigd in Januari 2019 te selecteren.
    
    De mogelijke weegmethoden voor datum vergelijking zijn:
     - after -> Datums groter en gelijk aan
     - before -> Datums kleiner en gelijk aan
     - strictly_after -> Datums groter dan
     - strictly_before -> Datums kleiner dan                                               
    
    Daarnaast kunnen de resultaten ook worden gesorteerd, dit gaat aan de hand van de order parameter, gevolgd door desc (aflopend) of asc (oplopend). Er kunnen meerdere order parameters op volgorde van toepassing worden meegegeven (bijvoorbeeld https://agenda.demo.zaakonline.nl/agenda?order[bronOrganisatie]=desc&order[identificatie]=asc)
    
    Kijk voor meer details over de mogelijkheden voor filteren en sorteren bij de GET (verzamel) endpoint van de entiteit die je wilt ophalen.
    
    # Scopes
    Applicaties kunnen op deze API alleen wijzigingen uitvoeren waarvoor zij over de juiste rechten(scopes) beschikken. De op deze api beschikbare scopes zijn:
     - agendas:schrijven
     - agendas:lezen
     - agendas:maken
     - agendas:verwijderen
     - taak:schrijven
     - taak:lezen
     - taak:maken
     - taak:verwijderen
     - afspraak:schrijven
     - afspraak:lezen
     - afspraak:maken
     - afspraak:verwijderen
     - beschikbaar:schrijven
     - beschikbaar:lezen
     - beschikbaar:maken
     - beschikbaar:verwijderen
     - applicatie:schrijven
     - applicatie:lezen
     - applicatie:maken
     - applicatie:inloggen        
             
    # Authentication
    Voor het gebruik van deze applicatie is een JWT Token nodig, deze hangt altijd vast aan een applicatie (ofwel user). Geldige JWT Tokens worden aangemaakt bij het registreren van een applicatie en kennen een beperkte houdbaarheid. 
    
    JWT Tokens kunnen zowel worden verlengd, als opnieuw worden aangevraagd, dit aan de hand van de applicatie id en sleutel.
    
    Let er bij het meegeven van JWT tokens op dat deze moet worden voorafgegaan door een Bearer en een spatie, onder de header Authorization. De volledig naam wordt daarmee: 'Authorization: Bearer [TOKEN]'.
    
  version: '0.0.20 Alpha'
  title: 'Agendas API'
  termsOfService: 'http://www.zaakonline.nl/terms/'
  contact:
    name: 'API Support'
    email: support@conduction.nl
    url: 'https://github.com/ConductionNL/zaakonline'
  x-logo:
    url: /img/vng.svg
    altText: 'VNG Realisatie'
  license:
    name: 'EUPL 1.2'
    url: 'https://opensource.org/licenses/EUPL-1.2'
paths:
  /afspraak:
    get:
      summary: 'Haal een verzameling van Afspraken op'
      tags:
        - Afspraak
      operationId: getAfspraakCollection
      responses:
        200:
          description: 'Afspraak collection response'
          content:
            application/ld+json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Afspraak'
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Afspraak'
            text/html:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Afspraak'
      parameters: []
    post:
      summary: 'Maak een Afspraak aan'
      tags:
        - Afspraak
      operationId: postAfspraakCollection
      responses:
        201:
          description: 'Afspraak resource created'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Afspraak'
            application/json:
              schema:
                $ref: '#/components/schemas/Afspraak'
            text/html:
              schema:
                $ref: '#/components/schemas/Afspraak'
          links:
            GetAfspraakItem:
              parameters:
                id: '$response.body#/id'
              operationId: getAfspraakItem
              description: 'The `id` value returned in the response can be used as the `id` parameter in `GET /afspraken/{id}`.'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Afspraak'
          application/json:
            schema:
              $ref: '#/components/schemas/Afspraak'
          text/html:
            schema:
              $ref: '#/components/schemas/Afspraak'
        description: 'The new Afspraak resource'
  '/afspraken/{id}':
    get:
      summary: 'Haal een specifieke Afspraak op'
      tags:
        - Afspraak
      operationId: getAfspraakItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Afspraak resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Afspraak'
            application/json:
              schema:
                $ref: '#/components/schemas/Afspraak'
            text/html:
              schema:
                $ref: '#/components/schemas/Afspraak'
        404:
          description: 'Resource not found'
    put:
      summary: 'Vervang een specifieke Afspraak'
      tags:
        - Afspraak
      operationId: putAfspraakItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Afspraak resource updated'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Afspraak'
            application/json:
              schema:
                $ref: '#/components/schemas/Afspraak'
            text/html:
              schema:
                $ref: '#/components/schemas/Afspraak'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Afspraak'
          application/json:
            schema:
              $ref: '#/components/schemas/Afspraak'
          text/html:
            schema:
              $ref: '#/components/schemas/Afspraak'
        description: 'The updated Afspraak resource'
    delete:
      summary: 'Verwijder een specifieke Afspraak'
      tags:
        - Afspraak
      operationId: deleteAfspraakItem
      responses:
        204:
          description: 'Afspraak resource deleted'
        404:
          description: 'Resource not found'
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
  '/afspraken/{id}/log':
    get:
      summary: 'Logboek inzien'
      description: 'Geeft een array van eerdere versies en wijzigingen van deze Afspraak'
      consumes:
        - application/json
        - text/html
      tags:
        - Afspraak
      operationId: logAfspraakItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Afspraak resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Afspraak-read'
            application/json:
              schema:
                $ref: '#/components/schemas/Afspraak-read'
            text/html:
              schema:
                $ref: '#/components/schemas/Afspraak-read'
        404:
          description: 'Resource not found'
  '/afspraken/{id}/revert/{version}':
    post:
      summary: 'Versie herstellen'
      description: 'Herstel een eerdere versie van deze Afspraak. Dit is een destructieve actie die niet ongedaan kan worden gemaakt'
      consumes:
        - application/json
        - text/html
      produces:
        - application/json
      responses:
        202:
          description: 'Hersteld naar eerdere versie'
        400:
          description: 'Ongeldige aanvraag'
        404:
          description: 'Afspraak niet gevonden'
      tags:
        - Afspraak
      operationId: revertAfspraakItem
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Afspraak-write'
          application/json:
            schema:
              $ref: '#/components/schemas/Afspraak-write'
          text/html:
            schema:
              $ref: '#/components/schemas/Afspraak-write'
        description: 'The new Afspraak resource'
  /agenda:
    get:
      summary: 'Haal een verzameling van Agendas op'
      tags:
        - Agenda
      operationId: getAgendaCollection
      responses:
        200:
          description: 'Agenda collection response'
          content:
            application/ld+json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Agenda'
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Agenda'
            text/html:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Agenda'
      parameters:
        -
          name: bronOrganisatie
          in: query
          required: false
          schema:
            type: integer
        -
          name: 'bronOrganisatie[]'
          in: query
          required: false
          schema:
            type: array
            items:
              type: integer
          style: form
          explode: true
        -
          name: 'order[bronOrganisatie]'
          in: query
          required: false
          schema:
            type: string
    post:
      summary: 'Maak een Agenda aan'
      tags:
        - Agenda
      operationId: postAgendaCollection
      responses:
        201:
          description: 'Agenda resource created'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Agenda'
            application/json:
              schema:
                $ref: '#/components/schemas/Agenda'
            text/html:
              schema:
                $ref: '#/components/schemas/Agenda'
          links:
            GetAgendaItem:
              parameters:
                id: '$response.body#/id'
              operationId: getAgendaItem
              description: 'The `id` value returned in the response can be used as the `id` parameter in `GET /agendas/{id}`.'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Agenda'
          application/json:
            schema:
              $ref: '#/components/schemas/Agenda'
          text/html:
            schema:
              $ref: '#/components/schemas/Agenda'
        description: 'The new Agenda resource'
  /agenda/match:
    get:
      summary: 'Vergelijk beschikbaarheid'
      description: 'Vergelijk een aantal Agenda''s en geef een lijst van matchende beschikbaarheid terug'
      consumes:
        - application/json
        - text/html
      produces:
        - application/json
      responses:
        200:
          description: 'successful operation'
        400:
          description: 'Ongeldige aanvraag'
        404:
          description: 'Agenda niet gevonden'
      tags:
        - Agenda
      operationId: matchAgendaCollection
      parameters:
        -
          name: bronOrganisatie
          in: query
          required: false
          schema:
            type: integer
        -
          name: 'bronOrganisatie[]'
          in: query
          required: false
          schema:
            type: array
            items:
              type: integer
          style: form
          explode: true
        -
          name: 'order[bronOrganisatie]'
          in: query
          required: false
          schema:
            type: string
  '/agendas/{id}':
    get:
      summary: 'Haalt een specifieke Agenda op'
      tags:
        - Agenda
      operationId: getAgendaItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Agenda resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Agenda'
            application/json:
              schema:
                $ref: '#/components/schemas/Agenda'
            text/html:
              schema:
                $ref: '#/components/schemas/Agenda'
        404:
          description: 'Resource not found'
    put:
      summary: 'Vervang een specifieke Agenda'
      tags:
        - Agenda
      operationId: putAgendaItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Agenda resource updated'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Agenda'
            application/json:
              schema:
                $ref: '#/components/schemas/Agenda'
            text/html:
              schema:
                $ref: '#/components/schemas/Agenda'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Agenda'
          application/json:
            schema:
              $ref: '#/components/schemas/Agenda'
          text/html:
            schema:
              $ref: '#/components/schemas/Agenda'
        description: 'The updated Agenda resource'
    delete:
      summary: 'Verwijder een specifieke Agenda'
      tags:
        - Agenda
      operationId: deleteAgendaItem
      responses:
        204:
          description: 'Agenda resource deleted'
        404:
          description: 'Resource not found'
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
  '/agendas/{id}/beschikbaar':
    get:
      summary: 'Controleer beschikbaarheid'
      description: 'Controleer welke periodes nog beschikbaar zijn in deze Agenda gedurende een gegeven periode'
      consumes:
        - application/json
        - text/html
      tags:
        - Agenda
      operationId: availabilityAgendaItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Agenda resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Agenda-read'
            application/json:
              schema:
                $ref: '#/components/schemas/Agenda-read'
            text/html:
              schema:
                $ref: '#/components/schemas/Agenda-read'
        404:
          description: 'Resource not found'
  '/agendas/{id}/log':
    get:
      summary: 'Logboek inzien'
      description: 'Geeft een array van eerdere versies en wijzigingen van deze Agenda'
      consumes:
        - application/json
        - text/html
      tags:
        - Agenda
      operationId: logAgendaItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Agenda resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Agenda-read'
            application/json:
              schema:
                $ref: '#/components/schemas/Agenda-read'
            text/html:
              schema:
                $ref: '#/components/schemas/Agenda-read'
        404:
          description: 'Resource not found'
  '/agendas/{id}/revert/{version}':
    post:
      summary: 'Versie herstellen'
      description: 'Herstel een eerdere versie van deze Agenda. Dit is een destructieve actie die niet ongedaan kan worden gemaakt'
      consumes:
        - application/json
        - text/html
      produces:
        - application/json
      responses:
        202:
          description: 'Teruggedraaid naar eerdere versie'
        400:
          description: 'Ongeldige aanvraag'
        404:
          description: 'Agenda niet gevonden'
      tags:
        - Agenda
      operationId: revertAgendaItem
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Agenda-write'
          application/json:
            schema:
              $ref: '#/components/schemas/Agenda-write'
          text/html:
            schema:
              $ref: '#/components/schemas/Agenda-write'
        description: 'The new Agenda resource'
  '/applicatie/{id}':
    get:
      summary: 'Haal op'
      description: 'Haal een Applicatie op.'
      tags:
        - Applicatie
      operationId: getApplicatieItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Applicatie resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Applicatie'
            application/json:
              schema:
                $ref: '#/components/schemas/Applicatie'
            text/html:
              schema:
                $ref: '#/components/schemas/Applicatie'
        404:
          description: 'Resource not found'
    put:
      summary: 'Werk bij'
      description: 'Werk een Applicatie bij.'
      tags:
        - Applicatie
      operationId: putApplicatieItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Applicatie resource updated'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Applicatie'
            application/json:
              schema:
                $ref: '#/components/schemas/Applicatie'
            text/html:
              schema:
                $ref: '#/components/schemas/Applicatie'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Applicatie'
          application/json:
            schema:
              $ref: '#/components/schemas/Applicatie'
          text/html:
            schema:
              $ref: '#/components/schemas/Applicatie'
        description: 'The updated Applicatie resource'
  '/applicatie/{id}/log':
    get:
      summary: Logboek
      description: 'Bekijk de wijzigingen op dit Applicatie object.'
      tags:
        - Applicatie
      operationId: logApplicatieItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Applicatie resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Applicatie-applicatie:lezen'
            application/json:
              schema:
                $ref: '#/components/schemas/Applicatie-applicatie:lezen'
            text/html:
              schema:
                $ref: '#/components/schemas/Applicatie-applicatie:lezen'
        404:
          description: 'Resource not found'
  /applicaties:
    get:
      summary: Verzameling
      description: 'Haal een verzameling van Applicaties op, het is mogelijk om deze resultaten te filteren aan de hand van query parameters. <br><br>Lees meer over het filteren van resultaten onder [filteren](/#section/Filteren).'
      tags:
        - Applicatie
      operationId: getApplicatieCollection
      responses:
        200:
          description: 'Applicatie collection response'
          content:
            application/ld+json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Applicatie'
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Applicatie'
            text/html:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Applicatie'
      parameters:
        -
          name: id
          in: query
          required: false
          schema:
            type: integer
        -
          name: 'id[]'
          in: query
          required: false
          schema:
            type: array
            items:
              type: integer
          style: form
          explode: true
        -
          name: naam
          in: query
          required: false
          schema:
            type: string
        -
          name: organisatie
          in: query
          required: false
          schema:
            type: integer
        -
          name: 'organisatie[]'
          in: query
          required: false
          schema:
            type: array
            items:
              type: integer
          style: form
          explode: true
        -
          name: contactPersoon
          in: query
          required: false
          schema:
            type: string
        -
          name: 'contactPersoon[]'
          in: query
          required: false
          schema:
            type: array
            items:
              type: string
          style: form
          explode: true
        -
          name: 'order[id]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'order[naam]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'order[organisatie]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'order[isActief]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'order[registratiedatum]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'order[wijzigingsdatum]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'order[contactPersoon]'
          in: query
          required: false
          schema:
            type: string
        -
          name: isActief
          in: query
          required: false
          schema:
            type: boolean
        -
          name: 'registratiedatum[before]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'registratiedatum[strictly_before]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'registratiedatum[after]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'registratiedatum[strictly_after]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'wijzigingsdatum[before]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'wijzigingsdatum[strictly_before]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'wijzigingsdatum[after]'
          in: query
          required: false
          schema:
            type: string
        -
          name: 'wijzigingsdatum[strictly_after]'
          in: query
          required: false
          schema:
            type: string
  /beschikbaar:
    get:
      summary: 'Haal een verzameling van Beschikbaarheden op'
      tags:
        - Beschikbaar
      operationId: getBeschikbaarCollection
      responses:
        200:
          description: 'Beschikbaar collection response'
          content:
            application/ld+json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Beschikbaar'
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Beschikbaar'
            text/html:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Beschikbaar'
      parameters: []
    post:
      summary: 'Maak een Beschikbaarheid aan'
      tags:
        - Beschikbaar
      operationId: postBeschikbaarCollection
      responses:
        201:
          description: 'Beschikbaar resource created'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Beschikbaar'
            application/json:
              schema:
                $ref: '#/components/schemas/Beschikbaar'
            text/html:
              schema:
                $ref: '#/components/schemas/Beschikbaar'
          links:
            GetBeschikbaarItem:
              parameters:
                id: '$response.body#/id'
              operationId: getBeschikbaarItem
              description: 'The `id` value returned in the response can be used as the `id` parameter in `GET /beschikbaarheden/{id}`.'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Beschikbaar'
          application/json:
            schema:
              $ref: '#/components/schemas/Beschikbaar'
          text/html:
            schema:
              $ref: '#/components/schemas/Beschikbaar'
        description: 'The new Beschikbaar resource'
  '/beschikbaarheden/{id}':
    get:
      summary: 'Haal een specifieke Beschikbaarheid op'
      tags:
        - Beschikbaar
      operationId: getBeschikbaarItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Beschikbaar resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Beschikbaar'
            application/json:
              schema:
                $ref: '#/components/schemas/Beschikbaar'
            text/html:
              schema:
                $ref: '#/components/schemas/Beschikbaar'
        404:
          description: 'Resource not found'
    put:
      summary: 'Vervang een specifieke Beschikbaarheid'
      tags:
        - Beschikbaar
      operationId: putBeschikbaarItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Beschikbaar resource updated'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Beschikbaar'
            application/json:
              schema:
                $ref: '#/components/schemas/Beschikbaar'
            text/html:
              schema:
                $ref: '#/components/schemas/Beschikbaar'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Beschikbaar'
          application/json:
            schema:
              $ref: '#/components/schemas/Beschikbaar'
          text/html:
            schema:
              $ref: '#/components/schemas/Beschikbaar'
        description: 'The updated Beschikbaar resource'
    delete:
      summary: 'Verwijder een Beschikbaarheid'
      tags:
        - Beschikbaar
      operationId: deleteBeschikbaarItem
      responses:
        204:
          description: 'Beschikbaar resource deleted'
        404:
          description: 'Resource not found'
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
  '/beschikbaarheden/{id}/log':
    get:
      summary: 'Logboek inzien'
      description: 'Geeft een array van eerdere versies en wijzigingen van deze Beschikbaarheid'
      consumes:
        - application/json
        - text/html
      tags:
        - Beschikbaar
      operationId: logBeschikbaarItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Beschikbaar resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Beschikbaar-read'
            application/json:
              schema:
                $ref: '#/components/schemas/Beschikbaar-read'
            text/html:
              schema:
                $ref: '#/components/schemas/Beschikbaar-read'
        404:
          description: 'Resource not found'
  '/beschikbaarheden/{id}/revert/{version}':
    post:
      summary: 'Versie herstellen'
      description: 'Herstel een eerdere versie van deze Beschikbaarheid. Dit is een destructieve actie die niet ongedaan kan worden gemaakt'
      consumes:
        - application/json
        - text/html
      produces:
        - application/json
      responses:
        202:
          description: 'Hersteld naar eerdere versie'
        400:
          description: 'Ongeldige aanvraag'
        404:
          description: 'Beschikbaarheid niet gevonden'
      tags:
        - Beschikbaar
      operationId: revertBeschikbaarItem
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Beschikbaar-write'
          application/json:
            schema:
              $ref: '#/components/schemas/Beschikbaar-write'
          text/html:
            schema:
              $ref: '#/components/schemas/Beschikbaar-write'
        description: 'The new Beschikbaar resource'
  /login_check:
    post:
      summary: 'Token halen'
      description: 'Inloggen als Applicatie en JWT Token ophalen.'
      tags:
        - Applicatie
      operationId: loginApplicatieCollection
      responses:
        201:
          description: 'Applicatie resource created'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Applicatie-applicatie:lezen'
            application/json:
              schema:
                $ref: '#/components/schemas/Applicatie-applicatie:lezen'
            text/html:
              schema:
                $ref: '#/components/schemas/Applicatie-applicatie:lezen'
          links:
            GetApplicatieItem:
              parameters:
                id: '$response.body#/id'
              operationId: getApplicatieItem
              description: 'The `id` value returned in the response can be used as the `id` parameter in `GET /applicatie/{id}`.'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Applicatie-applicatie:inloggen'
          application/json:
            schema:
              $ref: '#/components/schemas/Applicatie-applicatie:inloggen'
          text/html:
            schema:
              $ref: '#/components/schemas/Applicatie-applicatie:inloggen'
        description: 'The new Applicatie resource'
  /registreer:
    post:
      summary: Registreren
      description: 'Registreer een nieuwe Applicatie voor dit component.'
      tags:
        - Applicatie
      operationId: registerApplicatieCollection
      responses:
        201:
          description: 'Applicatie resource created'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Applicatie-applicatie:lezen'
            application/json:
              schema:
                $ref: '#/components/schemas/Applicatie-applicatie:lezen'
            text/html:
              schema:
                $ref: '#/components/schemas/Applicatie-applicatie:lezen'
          links:
            GetApplicatieItem:
              parameters:
                id: '$response.body#/id'
              operationId: getApplicatieItem
              description: 'The `id` value returned in the response can be used as the `id` parameter in `GET /applicatie/{id}`.'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Applicatie-applicatie:maken'
          application/json:
            schema:
              $ref: '#/components/schemas/Applicatie-applicatie:maken'
          text/html:
            schema:
              $ref: '#/components/schemas/Applicatie-applicatie:maken'
        description: 'The new Applicatie resource'
  /taak:
    get:
      summary: 'Haal een verzameling van Taken op'
      tags:
        - Taak
      operationId: getTaakCollection
      responses:
        200:
          description: 'Taak collection response'
          content:
            application/ld+json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Taak'
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Taak'
            text/html:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Taak'
      parameters: []
    post:
      summary: 'Maak een Taak aan'
      tags:
        - Taak
      operationId: postTaakCollection
      responses:
        201:
          description: 'Taak resource created'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Taak'
            application/json:
              schema:
                $ref: '#/components/schemas/Taak'
            text/html:
              schema:
                $ref: '#/components/schemas/Taak'
          links:
            GetTaakItem:
              parameters:
                id: '$response.body#/id'
              operationId: getTaakItem
              description: 'The `id` value returned in the response can be used as the `id` parameter in `GET /taken/{id}`.'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Taak'
          application/json:
            schema:
              $ref: '#/components/schemas/Taak'
          text/html:
            schema:
              $ref: '#/components/schemas/Taak'
        description: 'The new Taak resource'
  '/taken/{id}':
    get:
      summary: 'Haal een specifieke Taak op'
      tags:
        - Taak
      operationId: getTaakItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Taak resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Taak'
            application/json:
              schema:
                $ref: '#/components/schemas/Taak'
            text/html:
              schema:
                $ref: '#/components/schemas/Taak'
        404:
          description: 'Resource not found'
    put:
      summary: 'Vervang een specifieke Taak'
      tags:
        - Taak
      operationId: putTaakItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Taak resource updated'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Taak'
            application/json:
              schema:
                $ref: '#/components/schemas/Taak'
            text/html:
              schema:
                $ref: '#/components/schemas/Taak'
        400:
          description: 'Invalid input'
        404:
          description: 'Resource not found'
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Taak'
          application/json:
            schema:
              $ref: '#/components/schemas/Taak'
          text/html:
            schema:
              $ref: '#/components/schemas/Taak'
        description: 'The updated Taak resource'
    delete:
      summary: 'Verwijder een specifieke Taak'
      tags:
        - Taak
      operationId: deleteTaakItem
      responses:
        204:
          description: 'Taak resource deleted'
        404:
          description: 'Resource not found'
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
  '/taken/{id}/log':
    get:
      summary: 'Logboek inzien'
      description: 'Geeft een array van eerdere versies en wijzigingen van deze Taak'
      consumes:
        - application/json
        - text/html
      tags:
        - Taak
      operationId: logTaakItem
      parameters:
        -
          name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 'Taak resource response'
          content:
            application/ld+json:
              schema:
                $ref: '#/components/schemas/Taak-read'
            application/json:
              schema:
                $ref: '#/components/schemas/Taak-read'
            text/html:
              schema:
                $ref: '#/components/schemas/Taak-read'
        404:
          description: 'Resource not found'
  '/taken/{id}/revert/{version}':
    post:
      summary: 'Versie herstellen'
      description: 'Herstel een eerdere versie van deze Taak. Dit is een destructieve actie die niet ongedaan kan worden gemaakt'
      consumes:
        - application/json
        - text/html
      produces:
        - application/json
      responses:
        202:
          description: 'Hersteld naar eerdere versie'
        400:
          description: 'Ongeldige aanvraag'
        404:
          description: 'Taak niet gevonden'
      tags:
        - Taak
      operationId: revertTaakItem
      requestBody:
        content:
          application/ld+json:
            schema:
              $ref: '#/components/schemas/Taak-write'
          application/json:
            schema:
              $ref: '#/components/schemas/Taak-write'
          text/html:
            schema:
              $ref: '#/components/schemas/Taak-write'
        description: 'The new Taak resource'
security:
  -
    apiKey: []
components:
  schemas:
    Afspraak:
      type: object
      description: Afspraak
      properties:
        id:
          description: 'Het identificatienummer van deze Afspraak <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          type: integer
        agendas:
          description: 'De agendas waar deze Afspraak in staat'
          type: array
          items:
            type: string
        naam:
          description: 'De naam van deze Afspraak <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        samenvatting:
          description: 'Een korte samenvattende tekst over deze Afspraak bedoeld ter introductie.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        beschrijving:
          description: 'Een uitgebreide beschrijvende tekst over deze Afspraak bedoeld ter verdere verduidelijking.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        van:
          description: 'Het begin van deze Afspraak.'
          type: string
          format: date-time
        tot:
          description: 'Het einde van deze Afspraak.'
          type: string
          format: date-time
        herhaal:
          description: 'Herhaal deze Afspraak.'
          type: boolean
        maandag:
          description: Maandag
          type: boolean
        dinsdag:
          description: Dinsdag
          type: boolean
        woensdag:
          description: Woensdag
          type: boolean
        donderdag:
          description: Donderdag
          type: boolean
        vrijdag:
          description: Vrijdag
          type: boolean
        zaterdag:
          description: Zaterdag
          type: boolean
        zondag:
          description: Zondag
          type: boolean
        herhaalVan:
          description: 'Herhaal deze Afspraak vanaf'
          type: string
          format: date-time
        herhaalTot:
          description: 'Herhaal deze Afspraak tot'
          type: string
          format: date-time
        registratiedatum:
          description: 'Het tijdstip waarop dit Afspraak object is aangemaakt'
          type: string
          format: date-time
        wijzigingsdatum:
          description: 'Het tijdstip waarop dit Afspraak object voor het laatst is gewijzigd.'
          type: string
          format: date-time
        eigenaar:
          description: 'Met eigenaar wordt bijgehouden welke  applicatie verantwoordelijk is voor het object, en daarvoor de rechten beheerd en uitgeeft. In die zin moet de eigenaar dan ook worden gezien in de trant van autorisatie en configuratie in plaats van als onderdeel van het datamodel.'
          type: string
        url:
          readOnly: true
      required:
        - naam
        - samenvatting
        - beschrijving
    Afspraak-read:
      type: object
      description: Afspraak
      properties:
        id:
          description: 'Het identificatienummer van deze Afspraak <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          type: integer
        agendas:
          description: 'De agendas waar deze Afspraak in staat'
          type: array
          items:
            type: string
        naam:
          description: 'De naam van deze Afspraak <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        samenvatting:
          description: 'Een korte samenvattende tekst over deze Afspraak bedoeld ter introductie.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        beschrijving:
          description: 'Een uitgebreide beschrijvende tekst over deze Afspraak bedoeld ter verdere verduidelijking.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        van:
          description: 'Het begin van deze Afspraak.'
          type: string
          format: date-time
        tot:
          description: 'Het einde van deze Afspraak.'
          type: string
          format: date-time
        herhaal:
          description: 'Herhaal deze Afspraak.'
          type: boolean
        maandag:
          description: Maandag
          type: boolean
        dinsdag:
          description: Dinsdag
          type: boolean
        woensdag:
          description: Woensdag
          type: boolean
        donderdag:
          description: Donderdag
          type: boolean
        vrijdag:
          description: Vrijdag
          type: boolean
        zaterdag:
          description: Zaterdag
          type: boolean
        zondag:
          description: Zondag
          type: boolean
        herhaalVan:
          description: 'Herhaal deze Afspraak vanaf'
          type: string
          format: date-time
        herhaalTot:
          description: 'Herhaal deze Afspraak tot'
          type: string
          format: date-time
        registratiedatum:
          description: 'Het tijdstip waarop dit Afspraak object is aangemaakt'
          type: string
          format: date-time
        wijzigingsdatum:
          description: 'Het tijdstip waarop dit Afspraak object voor het laatst is gewijzigd.'
          type: string
          format: date-time
        eigenaar:
          description: 'Met eigenaar wordt bijgehouden welke  applicatie verantwoordelijk is voor het object, en daarvoor de rechten beheerd en uitgeeft. In die zin moet de eigenaar dan ook worden gezien in de trant van autorisatie en configuratie in plaats van als onderdeel van het datamodel.'
          type: string
      required:
        - naam
        - samenvatting
        - beschrijving
    Afspraak-write:
      type: object
      description: Afspraak
      required:
        - naam
        - samenvatting
        - beschrijving
      properties:
        naam:
          description: 'De naam van deze Afspraak <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        samenvatting:
          description: 'Een korte samenvattende tekst over deze Afspraak bedoeld ter introductie.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        beschrijving:
          description: 'Een uitgebreide beschrijvende tekst over deze Afspraak bedoeld ter verdere verduidelijking.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
    Agenda:
      type: object
      description: Agenda
      properties:
        id:
          description: 'Het identificatienummer van deze Agenda <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          type: integer
        identificatie:
          type: string
          example: 6a36c2c4-213e-4348-a467-dfa3a30f64aa
          description: 'De unieke identificatie van deze Agenda binnen de organisatie die ddeze Agenda heeft gecreeerd. <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          maxLength: 40
        bronOrganisatie:
          title: bronOrganisatie
          type: string
          example: '123456789'
          required: 'true'
          maxLength: 9
          minLength: 8
          description: 'Het RSIN van de organisatie waartoe deze Agenda behoort. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef. <br> Het RSIN wordt bepaald aan de hand van de geauthenticeerde applicatie en kan niet worden overschreven'
        naam:
          description: 'De naam van deze Agenda <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        beschrijving:
          description: 'Een beschrijvende tekst over deze Agenda  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        beschikbaarheid:
          description: 'De beschikbare momenten van deze Agenda.'
          type: array
          items:
            type: string
        afspraken:
          description: 'De in deze Agenda opgenomen afspraken.'
          type: array
          items:
            type: string
        taken:
          description: 'De in deze Agenda opgenomen taken.'
          type: array
          items:
            type: string
        registratiedatum:
          description: 'Het tijdstip waarop dit Agenda object is aangemaakt'
          type: string
          format: date-time
        wijzigingsdatum:
          description: 'Het tijdstip waarop dit Agenda object voor het laatst is gewijzigd.'
          type: string
          format: date-time
        eigenaar:
          description: 'Met eigenaar wordt bijgehouden welke  applicatie verantwoordelijk is voor het object, en daarvoor de rechten beheerd en uitgeeft. In die zin moet de eigenaar dan ook worden gezien in de trant van autorisatie en configuratie in plaats van als onderdeel van het datamodel.'
          type: string
        url:
          readOnly: true
      required:
        - naam
        - beschrijving
    Agenda-match:
      type: object
      description: Agenda
    Agenda-read:
      type: object
      description: Agenda
      properties:
        id:
          description: 'Het identificatienummer van deze Agenda <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          type: integer
        identificatie:
          type: string
          example: 6a36c2c4-213e-4348-a467-dfa3a30f64aa
          description: 'De unieke identificatie van deze Agenda binnen de organisatie die ddeze Agenda heeft gecreeerd. <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          maxLength: 40
        bronOrganisatie:
          title: bronOrganisatie
          type: string
          example: '123456789'
          required: 'true'
          maxLength: 9
          minLength: 8
          description: 'Het RSIN van de organisatie waartoe deze Agenda behoort. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef. <br> Het RSIN wordt bepaald aan de hand van de geauthenticeerde applicatie en kan niet worden overschreven'
        naam:
          description: 'De naam van deze Agenda <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        beschikbaarheid:
          description: 'De beschikbare momenten van deze Agenda.'
          type: array
          items:
            type: string
        afspraken:
          description: 'De in deze Agenda opgenomen afspraken.'
          type: array
          items:
            type: string
        taken:
          description: 'De in deze Agenda opgenomen taken.'
          type: array
          items:
            type: string
        registratiedatum:
          description: 'Het tijdstip waarop dit Agenda object is aangemaakt'
          type: string
          format: date-time
        wijzigingsdatum:
          description: 'Het tijdstip waarop dit Agenda object voor het laatst is gewijzigd.'
          type: string
          format: date-time
        eigenaar:
          description: 'Met eigenaar wordt bijgehouden welke  applicatie verantwoordelijk is voor het object, en daarvoor de rechten beheerd en uitgeeft. In die zin moet de eigenaar dan ook worden gezien in de trant van autorisatie en configuratie in plaats van als onderdeel van het datamodel.'
          type: string
      required:
        - naam
    Agenda-write:
      type: object
      description: Agenda
      properties:
        identificatie:
          type: string
          example: 6a36c2c4-213e-4348-a467-dfa3a30f64aa
          description: 'De unieke identificatie van deze Agenda binnen de organisatie die ddeze Agenda heeft gecreeerd. <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          maxLength: 40
        naam:
          description: 'De naam van deze Agenda <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
      required:
        - naam
    Applicatie:
      type: object
      description: 'Een Applicatie die is geidentificeerd en geautoriceerd om namens een organisatie wijzigingen uit te voeren'
      properties:
        id:
          type: integer
        naam:
          type: naam
          maxLength: 255
          minLength: 8
          example: mijn-applicatie
        sleutel:
          type: string
          maxLength: 16
          minLength: 5
          example: NietZoGeheim
          description: 'Een door de organisatie opgegeven sleutel waarmee deze Applicatie zich identificeert bij het ophalen van en JWT token.'
        scopes:
          type: array
          example: '[]'
          description: 'De scopes (rechten) die deze Applicatie heeft. Zie [scopes](/#section/Scopes) voor meer informatie.'
        organisatie:
          type: string
          example: '123456789'
          required: 'true'
          maxLength: 9
          minLength: 8
          description: 'Het RSIN van de organisatie waartoe deze Applicatie behoort. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef.'
        isActief:
          description: 'Wordt gebruikt om aan te geven of deze applicatie actief is (en mag inloggen) of dat deze slechts wordt gebruikt voor archief doeleinden.'
          type: boolean
        registratiedatum:
          description: 'Het tijdstip waarop deze Applicatie is aangemaakt.'
          type: string
          format: date-time
        wijzigingsdatum:
          description: 'Het tijdstip waarop deze Applicatie voor het laatst is gewijzigd.'
          type: string
          format: date-time
        contactPersoon:
          title: Ambtenaar
          type: url
          example: 'https://ref.tst.vng.cloud/zrc/api/v1/zaken/24524f1c-1c14-4801-9535-22007b8d1b65'
          required: 'true'
          maxLength: 255
          format: uri
          description: 'De contactpersoon voor deze Applicatie, die bijvoorbeeld wordt verwittigd bij misbruik.'
        fullname:
          readOnly: true
          type: string
        user:
          readOnly: true
          type: boolean
        username:
          description: 'Username wordt door symfony gebruikt voor de gebruikersnaam maar in de context common ground component api gebruiken we hem niet en onderdrukken we hem door hem niet toe te wijzen aan groepen.'
        password:
          readOnly: true
        salt:
          readOnly: true
        roles:
          readOnly: true
    'Applicatie-applicatie:inloggen':
      type: object
      description: 'Een Applicatie die is geidentificeerd en geautoriceerd om namens een organisatie wijzigingen uit te voeren'
      properties:
        naam:
          type: naam
          maxLength: 255
          minLength: 8
          example: mijn-applicatie
        sleutel:
          type: string
          maxLength: 16
          minLength: 5
          example: NietZoGeheim
          description: 'Een door de organisatie opgegeven sleutel waarmee deze Applicatie zich identificeert bij het ophalen van en JWT token.'
    'Applicatie-applicatie:lezen':
      type: object
      description: 'Een Applicatie die is geidentificeerd en geautoriceerd om namens een organisatie wijzigingen uit te voeren'
      properties:
        id:
          type: integer
        naam:
          type: naam
          maxLength: 255
          minLength: 8
          example: mijn-applicatie
        organisatie:
          type: string
          example: '123456789'
          required: 'true'
          maxLength: 9
          minLength: 8
          description: 'Het RSIN van de organisatie waartoe deze Applicatie behoort. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef.'
        isActief:
          description: 'Wordt gebruikt om aan te geven of deze applicatie actief is (en mag inloggen) of dat deze slechts wordt gebruikt voor archief doeleinden.'
          type: boolean
        registratiedatum:
          description: 'Het tijdstip waarop deze Applicatie is aangemaakt.'
          type: string
          format: date-time
        wijzigingsdatum:
          description: 'Het tijdstip waarop deze Applicatie voor het laatst is gewijzigd.'
          type: string
          format: date-time
        contactPersoon:
          title: Ambtenaar
          type: url
          example: 'https://ref.tst.vng.cloud/zrc/api/v1/zaken/24524f1c-1c14-4801-9535-22007b8d1b65'
          required: 'true'
          maxLength: 255
          format: uri
          description: 'De contactpersoon voor deze Applicatie, die bijvoorbeeld wordt verwittigd bij misbruik.'
    'Applicatie-applicatie:maken':
      type: object
      description: 'Een Applicatie die is geidentificeerd en geautoriceerd om namens een organisatie wijzigingen uit te voeren'
      properties:
        naam:
          type: naam
          maxLength: 255
          minLength: 8
          example: mijn-applicatie
        sleutel:
          type: string
          maxLength: 16
          minLength: 5
          example: NietZoGeheim
          description: 'Een door de organisatie opgegeven sleutel waarmee deze Applicatie zich identificeert bij het ophalen van en JWT token.'
        scopes:
          type: array
          example: '[]'
          description: 'De scopes (rechten) die deze Applicatie heeft. Zie [scopes](/#section/Scopes) voor meer informatie.'
        organisatie:
          type: string
          example: '123456789'
          required: 'true'
          maxLength: 9
          minLength: 8
          description: 'Het RSIN van de organisatie waartoe deze Applicatie behoort. Dit moet een geldig RSIN zijn van 9 nummers en voldoen aan https://nl.wikipedia.org/wiki/Burgerservicenummer#11-proef.'
        contactPersoon:
          title: Ambtenaar
          type: url
          example: 'https://ref.tst.vng.cloud/zrc/api/v1/zaken/24524f1c-1c14-4801-9535-22007b8d1b65'
          required: 'true'
          maxLength: 255
          format: uri
          description: 'De contactpersoon voor deze Applicatie, die bijvoorbeeld wordt verwittigd bij misbruik.'
    Beschikbaar:
      type: object
      description: Beschikbaarheid
      properties:
        id:
          description: 'Het identificatienummer van deze Beschikbaarheid <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          type: integer
        agendas:
          description: 'De agendas waar deze Beschikbaarheid in staat.'
          type: array
          items:
            type: string
        naam:
          description: 'De naam van deze Beschikbaarheid <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        samenvatting:
          description: 'Een korte samenvattende tekst over deze Beschikbaarheid bedoeld ter introductie.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        beschrijving:
          description: 'Een uitgebreide beschrijvende tekst over deze Ambtenaar bedoeld ter verdere verduidelijking  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        van:
          description: 'Het begin van deze Beschikbaarheid.'
          type: string
          format: date-time
        tot:
          description: 'Het einde van deze Beschikbaarheid'
          type: string
          format: date-time
        herhaal:
          description: 'Herhaal deze Beschikbaarheid'
          type: boolean
        maandag:
          description: Maandag
          type: boolean
        dinsdag:
          description: Dinsdag
          type: boolean
        woensdag:
          description: Woensdag
          type: boolean
        donderdag:
          description: Donderdag
          type: boolean
        vrijdag:
          description: Vrijdag
          type: boolean
        zaterdag:
          description: Zaterdag
          type: boolean
        zondag:
          description: Zondag
          type: boolean
        herhaalVan:
          description: 'Herhaal deze Beschikbaarheid vanaf'
          type: string
          format: date-time
        herhaalTot:
          description: 'Herhaal deze Beschikbaarheid tot'
          type: string
          format: date-time
        registratiedatum:
          description: 'Het tijdstip waarop deze Beschikbaarheid is aangemaakt.'
          type: string
          format: date-time
        wijzigingsdatum:
          description: 'Het tijdstip waarop deze Beschikbaarheid voor het laatst is gewijzigd.'
          type: string
          format: date-time
        eigenaar:
          description: 'Met eigenaar wordt bijgehouden welke  applicatie verantwoordelijk is voor het object, en daarvoor de rechten beheerd en uitgeeft. In die zin moet de eigenaar dan ook worden gezien in de trant van autorisatie en configuratie in plaats van als onderdeel van het datamodel.'
          type: string
        url:
          readOnly: true
      required:
        - naam
        - samenvatting
        - beschrijving
    Beschikbaar-read:
      type: object
      description: Beschikbaarheid
      properties:
        id:
          description: 'Het identificatienummer van deze Beschikbaarheid <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          type: integer
        agendas:
          description: 'De agendas waar deze Beschikbaarheid in staat.'
          type: array
          items:
            type: string
        naam:
          description: 'De naam van deze Beschikbaarheid <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        samenvatting:
          description: 'Een korte samenvattende tekst over deze Beschikbaarheid bedoeld ter introductie.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        beschrijving:
          description: 'Een uitgebreide beschrijvende tekst over deze Ambtenaar bedoeld ter verdere verduidelijking  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        van:
          description: 'Het begin van deze Beschikbaarheid.'
          type: string
          format: date-time
        tot:
          description: 'Het einde van deze Beschikbaarheid'
          type: string
          format: date-time
        herhaal:
          description: 'Herhaal deze Beschikbaarheid'
          type: boolean
        maandag:
          description: Maandag
          type: boolean
        dinsdag:
          description: Dinsdag
          type: boolean
        woensdag:
          description: Woensdag
          type: boolean
        donderdag:
          description: Donderdag
          type: boolean
        vrijdag:
          description: Vrijdag
          type: boolean
        zaterdag:
          description: Zaterdag
          type: boolean
        zondag:
          description: Zondag
          type: boolean
        herhaalVan:
          description: 'Herhaal deze Beschikbaarheid vanaf'
          type: string
          format: date-time
        herhaalTot:
          description: 'Herhaal deze Beschikbaarheid tot'
          type: string
          format: date-time
        registratiedatum:
          description: 'Het tijdstip waarop deze Beschikbaarheid is aangemaakt.'
          type: string
          format: date-time
        wijzigingsdatum:
          description: 'Het tijdstip waarop deze Beschikbaarheid voor het laatst is gewijzigd.'
          type: string
          format: date-time
        eigenaar:
          description: 'Met eigenaar wordt bijgehouden welke  applicatie verantwoordelijk is voor het object, en daarvoor de rechten beheerd en uitgeeft. In die zin moet de eigenaar dan ook worden gezien in de trant van autorisatie en configuratie in plaats van als onderdeel van het datamodel.'
          type: string
      required:
        - naam
        - samenvatting
        - beschrijving
    Beschikbaar-write:
      type: object
      description: Beschikbaarheid
      required:
        - naam
        - samenvatting
        - beschrijving
      properties:
        naam:
          description: 'De naam van deze Beschikbaarheid <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        samenvatting:
          description: 'Een korte samenvattende tekst over deze Beschikbaarheid bedoeld ter introductie.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        beschrijving:
          description: 'Een uitgebreide beschrijvende tekst over deze Ambtenaar bedoeld ter verdere verduidelijking  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
    Taak:
      type: object
      description: taak
      properties:
        id:
          description: 'Het identificatienummer van deze Taak <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          type: integer
        agendas:
          description: 'De agendas waar deze Taken in staan'
          type: array
          items:
            type: string
        naam:
          description: 'De naam van deze Taak <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        samenvatting:
          description: 'Een korte samenvattende tekst over deze Taak bedoeld ter introductie.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        beschrijving:
          description: 'Een uitgebreide beschrijvende tekst over deze Taak bedoeld ter verdere verduidelijking  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        herhaalVan:
          description: 'Herhaal deze Taak vanaf'
          type: string
          format: date-time
        herhaalTot:
          description: 'Herhaal deze Taak tot'
          type: string
          format: date-time
        registratiedatum:
          description: 'Het tijdstip waarop deze Taak is aangemaakt'
          type: string
          format: date-time
        wijzigingsdatum:
          description: 'Het tijdstip waarop deze Taak voor het laatst is gewijzigd'
          type: string
          format: date-time
        eigenaar:
          description: 'De eigenaar (applicatie) van dit object, wordt bepaald aan de hand van de geauthenticeerde applicatie die de taak heeft aangemaakt'
          type: string
      required:
        - naam
        - samenvatting
        - beschrijving
    Taak-read:
      type: object
      description: taak
      properties:
        id:
          description: 'Het identificatienummer van deze Taak <br /><b>Schema:</b> <a href="https://schema.org/identifier">https://schema.org/identifier</a>'
          type: integer
        agendas:
          description: 'De agendas waar deze Taken in staan'
          type: array
          items:
            type: string
        naam:
          description: 'De naam van deze Taak <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        samenvatting:
          description: 'Een korte samenvattende tekst over deze Taak bedoeld ter introductie.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        beschrijving:
          description: 'Een uitgebreide beschrijvende tekst over deze Taak bedoeld ter verdere verduidelijking  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        herhaalVan:
          description: 'Herhaal deze Taak vanaf'
          type: string
          format: date-time
        herhaalTot:
          description: 'Herhaal deze Taak tot'
          type: string
          format: date-time
        registratiedatum:
          description: 'Het tijdstip waarop deze Taak is aangemaakt'
          type: string
          format: date-time
        wijzigingsdatum:
          description: 'Het tijdstip waarop deze Taak voor het laatst is gewijzigd'
          type: string
          format: date-time
        eigenaar:
          description: 'De eigenaar (applicatie) van dit object, wordt bepaald aan de hand van de geauthenticeerde applicatie die de taak heeft aangemaakt'
          type: string
      required:
        - naam
        - samenvatting
        - beschrijving
    Taak-write:
      type: object
      description: taak
      required:
        - naam
        - samenvatting
        - beschrijving
      properties:
        naam:
          description: 'De naam van deze Taak <br /><b>Schema:</b> <a href="https://schema.org/name">https://schema.org/name</a>'
          type: string
        samenvatting:
          description: 'Een korte samenvattende tekst over deze Taak bedoeld ter introductie.  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
        beschrijving:
          description: 'Een uitgebreide beschrijvende tekst over deze Taak bedoeld ter verdere verduidelijking  <br /><b>Schema:</b> <a href="https://schema.org/description">https://schema.org/description</a>'
          type: string
  securitySchemes:
    apiKey:
      type: apiKey
      in: header
      description: 'Value for the Authorization header'
      name: Authorization
servers:
  -
    url: //agenda.zaakonline.nl/
    description: 'Sandbox server'
externalDocs:
  description: 'Vind hier meer informatie over de zaakonline code base.'
  url: 'https://github.com/ConductionNL/zaakonline'
tags:
  -
    name: Agenda
    description: 'De agenda wordt gebruikt om een momenten in de tijd of een tijdspanne weer tegeven.'
  -
    name: Taak
    description: 'En taak of ''to do'' binnen een agenda.'
  -
    name: Afspraak
    description: 'Een afspraak behorende bij een of meerdere agendas.'
  -
    name: Beschikbaar
    description: 'Een moment van beschikbaarheid behorende bij een of meerdere agendas, totale beschikbaarheid wordt berekend op agenda niveau door beschikbaarheid en afspraken met elkaar te vergelijken.'
  -
    name: Applicatie
    description: 'Een software applicatie binnen een organisatie die gebruik maakt van een component voor het creëren, ophalen, bijwerken of verwijderen van gegevens. Applicaties kunnen worden geregistreerd om namens een organisatie te handelen op een component. In deze zin zijn applicaties dan ook onderdeel van het beheerproces, infrastructuur en configuratie van een component in plaats van het datamodel dat door het component wordt geleverd.'
x-tagGroups:
  -
    name: Agenda
    tags:
      - Agenda
      - Taak
      - Afspraak
      - Beschikbaar
      - Applicatie
